this is the repository where we dispose our code after completing your section
HOW TO USE IT:
1) clone the repository
2) copy each file to its dedicated folder for example: for the unsw_analyzer.py needs to be putten inside the unsw_nb15 dataset forlder
3) go insde each file and change the dataset roots from the code, unless you are using linux then it's fine but if you encounter any problems it's probably the rooting so just adjust that to your needs
4) execute and you are all set

#########################
how the workflow work:
after you finish with your code, copy and paste it back to the cloned repository and use the right commands to push the code to the github repo
don't forget to document your code so all of us can understand it:

much love, Abderrahmen Berradj
